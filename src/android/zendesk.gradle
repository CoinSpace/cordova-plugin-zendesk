repositories {
  maven { url "https://maven.google.com" }
  maven { url 'https://zendesk.jfrog.io/zendesk/repo' }
}

dependencies {
  compile(group: 'com.zendesk', name: 'support', version: '3.0.3')
}
